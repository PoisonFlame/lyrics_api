var CarbonIconsVue = {
  install: function install(Vue, options) {
    var components = options.components;
    Object.keys(components).forEach(function (key) {
      Vue.component(key, components[key]);
    });
  }
};

export { CarbonIconsVue };
